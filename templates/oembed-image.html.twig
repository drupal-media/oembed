{#
/**
 * Returns HTML for an oEmbed image.
 *
 * Available variables
 * - string provider: The name of the provider.
 * - string tag: The name of the HTML tag whose contents are being captioned.
 * - string caption: The caption text.
 * - string|NULL align: (optional) The alignment: 'left', 'center', 'right' or
 *   NULL.
 */
#}
<img class="oembed oembed-image oembed-provider-{{ provider|class }}" src="{{ url }}" height="{{ height }}" width="{{ width }}" alt="{{ title }}" />
